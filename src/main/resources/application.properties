# 服务端口（Railway 用 PORT 环境变量，本地默认 8080）
server.port=${PORT:8080}

# Thymeleaf 模板配置
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# 文件上传限制（导入 Excel 用）
spring.servlet.multipart.max-file-size=10MB
